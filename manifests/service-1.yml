apiVersion: v1
kind: Service
metadata:
    name: "kodeypetest-729c"
    labels:
        app: "kodeypetest-729c"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kodeypetest-729c"