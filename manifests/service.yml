apiVersion: v1
kind: Service
metadata:
    name: "kodeypetest-2f02"
    labels:
        app: "kodeypetest-2f02"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kodeypetest-2f02"