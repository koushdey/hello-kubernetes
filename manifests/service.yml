apiVersion: v1
kind: Service
metadata:
    name: "kodeypetest"
    labels:
        app: "kodeypetest"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kodeypetest"