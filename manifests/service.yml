apiVersion: v1
kind: Service
metadata:
    name: "kodeypetest-296e"
    labels:
        app: "kodeypetest-296e"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kodeypetest-296e"