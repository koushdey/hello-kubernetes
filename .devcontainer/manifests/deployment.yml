apiVersion : apps/v1beta1
kind: Deployment
metadata:
  name: kodeyaks
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: kodeyaks
    spec:
      containers:
        - name: kodeyaks
          image: kodeyacr.azurecr.io/kodeyaks
          ports:
          - containerPort: 80